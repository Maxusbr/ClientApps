<UserControl xmlns:Car="clr-namespace:DTCDev.Client.Cars.Controls.Controls.Car"  x:Class="DTCDev.Client.Cars.Controls.Controls.Car.CarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <ListBox ItemsSource="{Binding Path=CarsList}" SelectedItem="{Binding Path=SelectedCar, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Car:CarViewElement CarExemplar="{Binding}" AddCommand="{Binding Path=SelectCommand}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
