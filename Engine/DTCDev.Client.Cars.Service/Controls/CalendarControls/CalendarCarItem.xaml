<UserControl x:Class="DTCDev.Client.Cars.Service.Controls.CalendarControls.CalendarCarItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="170" Width="220"  >
    <Grid>
        <Grid Margin="10">
            <TextBlock Text="23" x:Name="txtDate" FontSize="60" FontWeight="Bold" Foreground="LightGray"/>
            <TextBlock x:Name="txtNoCar" Text=" Автомобилей нет" VerticalAlignment="Bottom" Foreground="Gray" FontStyle="Italic" />
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Background="{x:Null}" BorderBrush="{x:Null}" Margin="80,0,0,0">
                <StackPanel x:Name="stkCars">
                </StackPanel>
            </ScrollViewer>
        </Grid>
        </Grid>
</UserControl>
